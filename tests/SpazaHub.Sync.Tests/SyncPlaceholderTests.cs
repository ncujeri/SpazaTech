namespace SpazaHub.Sync.Tests;

/// <summary>
/// The sync engine (outbox, idempotent push, cursor pull, LWW conflicts, replay and
/// week-offline scenarios) is Phase 2. This project is scaffolded now so the test
/// pipeline shape is stable from day one.
/// </summary>
public class SyncPlaceholderTests
{
    [Fact]
    public void SyncProtocolTestsArriveInPhaseTwo()
    {
        Assert.True(true);
    }
}
